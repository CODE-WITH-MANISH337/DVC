1.create git repo and clone it in local 
2.create mycode.py and add the code to it . (it will save the csv file to new data folder)

3.do a git commit before initilazing the dvc

4.pip install dvc 

5.dvc init ( create file --> dvcignore,dvc)

6.Now do "mkdir S3" (create the S3 directory)

7.Now we do "dvc remote add -d myremote S3"

8.Now "dvc add data/"

9.git status ---> git add . --> git commit ---push to origin--->>make nothing to commit 


10 Now do "dvc commit" and then "git push"---> Now data added into the S3( 2 file(1->data(Od),2-->(md5(17))))

11.Now do git-commit-push origin main ( this time S3 will add ---> but in real case S3 is remote buck in aws)

12. Now make changes to code.py to append a new row in data, check changes via "dvc status"
13. Again - - "dvc commit" and then "dvc push"
14. Then git add-commit-push (we're saving V2 of our data at this point)
15. Check dvc/git status, everything should be upto date.
16. Now repeat step 10-12 for v3 of data.


# To rollback to same data and code 
git checkout <sha_id>
dvc pull